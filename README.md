# Simple backend for projectpilot
This launches json-server to act as a mock backend.
It reads data from api/db.json and creates a functional REST API at http://localhost:4000.
This allows you to develop your frontend against an API without needing a real server or database running.

## Start the server
```bash
npm install
npm run api
